[project]
name = "apply4me-pw"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.48.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["pw"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
